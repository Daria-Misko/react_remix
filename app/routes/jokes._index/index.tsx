import { Box, Typography } from "@mui/material";

export default function JokesIndexRoute() {
	return (
		<Box ml={10}>
			<Typography variant="h4">Here is a rendom joke:</Typography>
			<Typography variant="h5">
				What falls, but never needs a bandage? The rain.
			</Typography>
		</Box>
	);
}
